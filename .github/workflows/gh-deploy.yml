name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print Current Branch
        run: echo "Current branch is $GITHUB_REF"

      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Print Current Branch
        run: echo "Current branch is $GITHUB_REF"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 16

      - name: Install Dependencies
        run: npm ci

      - name: Debug Repository Contents Before Build
        run: |
          ls -R .

      - name: Build Angular App
        run: npm run gh-build

      - name: Debug Repository Contents After Build
        run: |
          ls -R .

      # - name: Debug Build Directory
      #   run: |
      #     ls -R ./dist

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Print Current Branch
        run: echo "Current branch is $GITHUB_REF"

      - name: Debug Repository Contents Before Checkout
        run: |
          ls -R .

      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Print Current Branch
        run: echo "Current branch is $GITHUB_REF"

      - name: Debug Repository Contents After Checkout
        run: |
          ls -R .

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist